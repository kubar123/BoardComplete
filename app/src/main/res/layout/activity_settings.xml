<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.JakubRybicki.assignment.MainActivity" >

    <ListView
        android:id="@+id/listCustIcon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/switchCustIcon"
        android:layout_below="@+id/switchCustIcon"
        android:visibility="invisible" >

    </ListView>

    <Switch
        android:id="@+id/switchCustIcon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/switchHardmode"
        android:layout_below="@+id/switchHardmode"
        android:layout_marginTop="24dp"
        android:checked="false"
        android:hint="6x6 grid"
        android:text="Use custom icons" />

    <Spinner
        android:id="@+id/spinDifficulty"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/switchHardmode"
        android:layout_alignParentTop="true" />

    <Switch
        android:id="@+id/switchHardmode"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/spinDifficulty"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:checked="false"
        android:hint="6x6 grid"
        android:text="Use hard mode" />

</RelativeLayout>
